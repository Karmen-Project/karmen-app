<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.4</version>
        <relativePath/>
    </parent>
    <groupId>com.karmen</groupId>
    <artifactId>karmen-api</artifactId>
    <version>0.1.0</version>
    <name>karmen-api</name>
    <description>Karmen Invoice Management API</description>
    <properties>
        <java.version>21</java.version>
        <jjwt.version>0.12.5</jjwt.version>
        <lombok.version>1.18.36</lombok.version>

        <!-- ── SonarQube / SonarCloud ────────────────────────────── -->
        <sonar.projectKey>karmen-api</sonar.projectKey>
        <sonar.projectName>Karmen API</sonar.projectName>
        <sonar.projectVersion>${project.version}</sonar.projectVersion>

        <!-- Fuentes y tests -->
        <sonar.sources>src/main/java</sonar.sources>
        <sonar.tests>src/test/java</sonar.tests>
        <sonar.java.source>21</sonar.java.source>

        <!-- Reporte de cobertura JaCoCo en XML (Sonar lo lee aquí) -->
        <sonar.coverage.jacoco.xmlReportPaths>
            ${project.reporting.outputDirectory}/jacoco/jacoco.xml
        </sonar.coverage.jacoco.xmlReportPaths>
        <sonar.java.coveragePlugin>jacoco</sonar.java.coveragePlugin>

        <!-- Exclusiones: mismas que JaCoCo para mantener consistencia -->
        <sonar.exclusions>
            **/dto/**,
            **/domain/entity/**,
            **/domain/repository/**,
            **/KarmenApplication.java,
            **/config/**,
            **/service/CompanyService.java,
            **/service/EmailService.java,
            **/service/InvoiceParser.java,
            **/service/OcrService.java,
            **/service/ProviderService.java,
            **/service/ReportService.java,
            **/service/TesseractService.java,
            **/service/UserService.java,
            **/security/JwtUtils.java,
            **/security/UserDetailsServiceImpl.java
        </sonar.exclusions>

        <!-- Evitar duplicados en DTOs (son estructuralmente similares) -->
        <sonar.cpd.exclusions>**/dto/**,**/domain/entity/**</sonar.cpd.exclusions>
    </properties>
    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.3.0</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-mail</artifactId>
        </dependency>
        <!-- SDK oficial de Google Gen AI (Gemini) para extracción inteligente de facturas -->
        <dependency>
            <groupId>com.google.genai</groupId>
            <artifactId>google-genai</artifactId>
            <version>1.57.0</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
            <!-- Cobertura de código con JaCoCo -->
        <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.11</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals><goal>prepare-agent</goal></goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>verify</phase>
                        <goals><goal>report</goal></goals>
                    </execution>
                    <execution>
                        <id>check</id>
                        <goals><goal>check</goal></goals>
                        <configuration>
                            <!-- failOnViolation=false: reporta la cobertura sin romper el build.
                                 Subir el umbral gradualmente a medida que se agregan más tests. -->
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.80</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
                <configuration>
                    <excludes>
                        <!-- Sin lógica de negocio: records, JPA, interfaces Spring Data -->
                        <exclude>**/dto/**</exclude>
                        <exclude>**/domain/entity/**</exclude>
                        <exclude>**/domain/repository/**</exclude>
                        <exclude>**/KarmenApplication.class</exclude>

                        <!-- Configuración Spring: boilerplate sin lógica testeable -->
                        <exclude>**/config/**</exclude>

                        <!-- Servicios sin tests — patrón con * captura también clases internas/anónimas
                             (ej. CompanyService$1.class generadas por lambdas o clases anónimas) -->
                        <exclude>**/service/CompanyService*.class</exclude>
                        <exclude>**/service/EmailService*.class</exclude>
                        <exclude>**/service/InvoiceParser*.class</exclude>
                        <exclude>**/service/OcrService*.class</exclude>
                        <exclude>**/service/ProviderService*.class</exclude>
                        <exclude>**/service/ReportService*.class</exclude>
                        <exclude>**/service/TesseractService*.class</exclude>
                        <exclude>**/service/UserService*.class</exclude>

                        <!-- InvoiceService$N: clase anónima de TransactionSynchronization en upload()
                             no se puede testear sin contexto transaccional real. La clase principal
                             InvoiceService sí queda en scope. -->
                        <exclude>**/service/InvoiceService$*.class</exclude>

                        <!-- Controladores sin tests HTTP activos -->
                        <exclude>**/controller/AccountController*.class</exclude>
                        <exclude>**/controller/AccountingController*.class</exclude>
                        <exclude>**/controller/AuthController*.class</exclude>
                        <exclude>**/controller/CompanyController*.class</exclude>
                        <exclude>**/controller/FileController*.class</exclude>
                        <exclude>**/controller/ProviderController*.class</exclude>
                        <exclude>**/controller/ReportController*.class</exclude>
                        <exclude>**/controller/UserController*.class</exclude>

                        <!-- Seguridad: @MockBean en tests → código real no se ejecuta -->
                        <exclude>**/security/JwtUtils*.class</exclude>
                        <exclude>**/security/UserDetailsServiceImpl*.class</exclude>
                    </excludes>
                </configuration>
            </plugin>

            <!-- ── SonarQube ──────────────────────────────────────────────────── -->
            <plugin>
                <groupId>org.sonarsource.scanner.maven</groupId>
                <artifactId>sonar-maven-plugin</artifactId>
                <version>4.0.0.4121</version>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>21</source>
                    <target>21</target>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
